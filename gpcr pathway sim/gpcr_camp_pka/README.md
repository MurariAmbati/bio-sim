# gpcr → camp / pka (gpcr_camp_pka)

## quickstart

```bash
cd "gpcr_camp_pka"
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python run_ode.py
```

```bash
python run_ssa.py --seed 2
```

## streamlit

```bash
streamlit run app.py
```
